#include <stdio.h>
#include <platform.h>

void chainElement( chanend cInput, chanend cOutput) {}

int main ( void ) {
  chan c[4];
  par (int i=0; i<4; i++)
     chainElement(c[i], c[(i+1)%4]);
  return 0;
}
